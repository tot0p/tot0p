<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - {{ author.name }}</title>
    <link rel="stylesheet" href="/css/style.css">
    <meta name="description" content="{{ description or author.bio[lang] }}">
    <link rel="canonical" href="https://tot0p.github.io{{ page.url }}">
</head>
<body>
    <header class="header">
        <nav class="nav">
            <div class="nav-brand">
                <a href="{% if lang == 'en' %}/en/{% else %}/fr/{% endif %}">{{ author.name }}</a>
            </div>
            <ul class="nav-links">
                <li><a href="{% if lang == 'en' %}/en/{% else %}/fr/{% endif %}">
                    {% if lang == 'en' %}Home{% else %}Accueil{% endif %}
                </a></li>
                <li><a href="{% if lang == 'en' %}/en/projects/{% else %}/fr/projets/{% endif %}">
                    {% if lang == 'en' %}Projects{% else %}Projets{% endif %}
                </a></li>
                <li><a href="{% if lang == 'en' %}/en/about/{% else %}/fr/à-propos/{% endif %}">
                    {% if lang == 'en' %}About{% else %}À propos{% endif %}
                </a></li>
            </ul>
            <div class="language-switcher">
                {% set currentPath = page.url | replace('/en/', '') | replace('/fr/', '') %}
                {% if lang == 'en' %}
                    {% set frenchUrl = '' %}
                    {% if currentPath == '' or currentPath == '/' %}
                        {% set frenchUrl = '/fr/' %}
                    {% elif currentPath == 'about/' %}
                        {% set frenchUrl = '/fr/à-propos/' %}
                    {% elif currentPath == 'projects/' %}
                        {% set frenchUrl = '/fr/projets/' %}
                    {% else %}
                        {% set frenchUrl = '/fr' + currentPath %}
                    {% endif %}
                    <a href="{{ frenchUrl }}" class="lang-link">FR</a>
                    <span class="lang-current">EN</span>
                {% else %}
                    {% set englishUrl = '' %}
                    {% if currentPath == '' or currentPath == '/' %}
                        {% set englishUrl = '/en/' %}
                    {% elif currentPath == 'à-propos/' %}
                        {% set englishUrl = '/en/about/' %}
                    {% elif currentPath == 'projets/' %}
                        {% set englishUrl = '/en/projects/' %}
                    {% else %}
                        {% set englishUrl = '/en' + currentPath %}
                    {% endif %}
                    <span class="lang-current">FR</span>
                    <a href="{{ englishUrl }}" class="lang-link">EN</a>
                {% endif %}
            </div>
        </nav>
    </header>

    <main class="main">
        <div class="container">
            {{ content | safe }}
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <p>&copy; 2025 {{ author.name }}. 
                    {% if lang == 'en' %}All rights reserved.{% else %}Tous droits réservés.{% endif %}
                </p>
                <div class="footer-links">
                    <a href="{{ author.github }}" target="_blank" rel="noopener noreferrer">GitHub</a>
                    <a href="{{ author.linkedin }}" target="_blank" rel="noopener noreferrer">LinkedIn</a>
                </div>
            </div>
        </div>
    </footer>
</body>
</html>
