<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - {{ author.name }}</title>
    <link rel="stylesheet" href="/css/style.css">
    <meta name="description" content="{{ description or author.bio[lang] }}">
    <link rel="canonical" href="https://tot0p.github.io{{ page.url }}">
</head>
<body>
    <header class="header">
        <nav class="nav">
            <div class="nav-brand">
                <a href="{% if lang == 'en' %}/{% else %}/fr/{% endif %}">{{ author.name }}</a>
            </div>
            <ul class="nav-links">
                <li><a href="{% if lang == 'en' %}/{% else %}/fr/{% endif %}">
                    {% if lang == 'en' %}Home{% else %}Accueil{% endif %}
                </a></li>
                <li><a href="{% if lang == 'en' %}/projects/{% else %}/fr/projets/{% endif %}">
                    {% if lang == 'en' %}Projects{% else %}Projets{% endif %}
                </a></li>
                <li><a href="{% if lang == 'en' %}/about/{% else %}/fr/à-propos/{% endif %}">
                    {% if lang == 'en' %}About{% else %}À propos{% endif %}
                </a></li>
            </ul>
            <div class="language-switcher">
                {% if lang == 'en' %}
                    <a href="/fr{{ page.url | replace('/en', '') }}" class="lang-link">FR</a>
                    <span class="lang-current">EN</span>
                {% else %}
                    <span class="lang-current">FR</span>
                    <a href="{{ page.url | replace('/fr', '') }}" class="lang-link">EN</a>
                {% endif %}
            </div>
        </nav>
    </header>

    <main class="main">
        <div class="container">
            {{ content | safe }}
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <p>&copy; 2025 {{ author.name }}. 
                    {% if lang == 'en' %}All rights reserved.{% else %}Tous droits réservés.{% endif %}
                </p>
                <div class="footer-links">
                    <a href="{{ author.github }}" target="_blank" rel="noopener noreferrer">GitHub</a>
                    <a href="{{ author.linkedin }}" target="_blank" rel="noopener noreferrer">LinkedIn</a>
                </div>
            </div>
        </div>
    </footer>
</body>
</html>
